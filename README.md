# Embedded AI Development Kit — Web

产品官网静态页：relay-c + ssh-bridge-c 分布式嵌入式调试架构展示。

## 本地预览

```bat
start index.html
```

## 部署

```powershell
.\deploy.ps1
```

默认部署到 `39.97.234.200`，站点根目录 `/var/www/embedded-ai-kit/`。

## 发布包

将 zip 放入 `downloads/`，更新 `downloads/manifest.json` 后重新 deploy。
